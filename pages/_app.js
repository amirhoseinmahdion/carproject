import Layout from '@amirmahdion/components/Layout'
import '@amirmahdion/styles/globals.css'

export default function App({ Component, pageProps }) {
  return (
  
    <Layout>
 <Component {...pageProps} />
 </Layout>
  
 
  
  
  
  ) 
}
